{
  "name": "live-stream-share-cast-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create livestream-db",
    "db:apply": "wrangler d1 execute livestream-db --file=./schema.sql",
    "db:apply-local": "wrangler d1 execute livestream-db --local --file=./schema.sql",
    "tail": "wrangler tail"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241101",
    "typescript": "^5.5.3",
    "wrangler": "^3.90.0"
  }
}
